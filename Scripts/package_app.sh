#!/usr/bin/env bash
#
# Release packaging — ad-hoc signed .zip / .dmg.
#
# STUB: the real implementation lands in Epic 1.6 (docs/PLAN.md):
#   1.6.1  Release build, ad-hoc sign (codesign --sign -), zip to dist/
#   1.6.2  create-dmg drag-to-Applications layout
#
# Kept as a stub so the path exists for 1.6.x without faking a release.
#
set -euo pipefail

echo "Scripts/package_app.sh is a stub — release packaging is implemented in Epic 1.6 (see docs/PLAN.md 1.6.1 / 1.6.2)." >&2
exit 1
